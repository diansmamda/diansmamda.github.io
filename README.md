# Hello, welcome to this project!

Cara commit ke repository:
1. Tambahkan file ke directory
1. Jalankan command `git add .` (ada titiknya)
1. Jalankan command `git commit -m "isi pesan commit"`
1. Jalankan command `git push origin main`
